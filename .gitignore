# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Streamlit secrets (never commit tokens)
.streamlit/secrets.toml

# Local runtime backups (the durable copies are pushed to GitHub via the API)
backups/
